spring:
  application:
    name: user_profile
  config:
    activate: 
      on-profile: default

# These settings are commonly used for easier monitoring and debugging in development or internal environments.
# For production, you may want to restrict endpoint exposure for security.
management:
  endpoint:
    shutdown:
      access: unrestricted # when user_profile shutsdown, this actuator endpoint helps this service to shut down gracefully
  info:
    env:
      enabled: true # enables the actuator's info endpoint
  endpoints:
    web:
      exposure:
        include: "*" # This exposes all Spring Boot Actuator endpoints over HTTP (e.g., /actuator/health, /actuator/info, /actuator/bus/refresh, etc.).
  endpoint:
    health:
      show-details: always # This configures the /actuator/health endpoint to always show detailed health information, not just the overall status. This is useful for debugging and monitoring, as you’ll see details about each health indicator (e.g., disk space, database, config server status).
      probes:
        enabled: true
  health:
    liveness-state: # determines if the application is alive
      enabled: true #localhost:8071/actuator/health/liveness
    readiness-state: # determines if the application is ready to accept/serve network requests
      enabled: true #localhost:8071/actuator/health/readiness

logging:
  level:
    root: DEBUG

endpoints:
  shutdown:
    enabled: true # enables actuator shutdown endpoint
    
# information  returned by /actuator/info
info:
  app:
    name: User Profile Service (Default)
    version: 1.0.0
    description: User Profile Service for Ai news Platform (default profile)

